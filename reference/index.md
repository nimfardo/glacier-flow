# Reference Index

> Catalog of deep docs **you** wrote about *this* project. Authoritative for project
> decisions and patterns. Loaded on demand — load only the named section, never the whole folder.

## Architecture

- [`architecture/`](architecture/) — empty until this project needs it. The first time a file placement decision matters, document the chosen pattern here (see `architecture/README.md`). Don't pre-fill this for a project that doesn't need the ceremony.

## Design System

*Add `design-system.md` here once this project has one (design tokens, component inventory). See `workspaces/design-to-code/CONTEXT.md` for how it's used.*

## Conventions

*Add files here as you write them (e.g. `api-conventions.md`, `data-model.md`, `ui-patterns.md`). Don't create them just to fill the table.*
